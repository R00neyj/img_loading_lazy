# Changelog

## v2.2.3
- **속성/스타일 선택 옵션 추가**: `width`, `height` 속성 및 `aspect-ratio` 스타일의 추가 여부를 개별적으로 선택할 수 있습니다.
- **인터페이스 통일**: 모든 CLI 질문의 응답 방식을 숫자 선택(1: 예, 2: 아니오) 방식으로 통일하여 편의성을 높였습니다.

## v2.2.1
- **기준 해상도 기본값 변경 (1920 → 1905)**: 최신 크롬 브라우저의 스크롤바 포함 영역 계산 방식 변경에 대응하여, `vw` 계산 기준 가로폭 기본값을 1905px로 조정하였습니다.
- **해상도 프리셋 추가**: CLI 실행 시 자주 사용되는 해상도(FHD, QHD, 4K)를 간편하게 선택할 수 있는 프리셋 옵션이 추가되었습니다.

## v2.2.0
- **height 속성 명시 제거**: `<img>` 태그에 `height` 속성을 직접 추가하는 대신, `style` 속성의 `aspect-ratio`를 통해 높이를 제어하도록 변경되었습니다. 이를 통해 반응형 대응 시 발생할 수 있는 높이 왜곡 문제를 방지하면서 CLS 방지 효과는 그대로 유지합니다.

## v2.1.0
- **Aspect Ratio 자동 주입**: 이미지의 가로/세로 비율을 계산하여 `style` 속성에 `aspect-ratio`를 자동으로 추가합니다. 이를 통해 이미지 로딩 전에도 정확한 영역을 확보하여 CLS를 더욱 완벽하게 방지합니다.

## v2.0.0
- **Eager 로딩 개별 지원**: `data-loading="eager"` 속성을 가진 부모 컨테이너 내의 이미지는 자동으로 `loading="eager"`가 적용됩니다. (LCP 이미지 최적화에 유용)
- **기본 엔진 변경**: 더욱 정교한 처리를 위해 `Cheerio` 엔진이 기본값으로 설정되었습니다.
- **태그 보존 고도화**: Cheerio 엔진 사용 시 빈 태그(`script`, `div` 등)의 닫는 태그가 누락되지 않도록 복원 로직이 강화되었습니다.

## v1.9.1
- **처리 완료 태그 추가 (`data-resized`)**: 이미지 크기 계산 및 변환이 성공적으로 완료된 `<img>` 태그에 `data-resized="true"` 속성을 자동으로 추가하여 중복 처리를 방지하고 상태를 명확히 구분할 수 있습니다.

## v1.9.0
- **듀얼 엔진 지원 (Regex / Cheerio)**: 기존의 안전한 정규표현식 엔진 외에 정교한 HTML 파싱이 가능한 Cheerio 엔진을 실험적으로 도입했습니다.
- **고도화된 구조 보존 기술**: `xmlMode`와 커스텀 복원 로직을 통해 `head`, `body` 태그 유실 및 `script` 태그가 깨지는 현상을 완벽히 해결했습니다.
- **CLI UX 개선**: 터미널 질문의 가독성을 높여 사용자가 설정을 더 쉽고 빠르게 마칠 수 있도록 최적화했습니다.

## v1.8.0
- **단위 생략 옵션 추가**: `none`을 선택하여 스타일 변환 없이 `width`, `height`, 레이지 속성만 추가 가능.
- **폴더 구조 최적화**: 루트 폴더를 사용자 작업 공간 위주로 정리(src 폴더 도입).
